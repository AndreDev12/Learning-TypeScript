"use strict";
// function addNumbers (x: number, y: number): number {
//     return x + y;
// }
// addNumbers(1, 2);
//  Funciones anónimas
let addNumbers = function (x, y) {
    return x + y;
};
addNumbers(1, 2);
